import React from 'react'

const BlackAnimation = () => {
    return (
        <div className="flex items-center gap-4 py-1.5">
            <span className="animate-ping inline-flex h-3 w-3 rounded-full bg-black opacity-100"></span>
            <span className="animate-ping inline-flex h-3 w-3 rounded-full bg-black opacity-100"></span>
            <span className="animate-ping inline-flex h-3 w-3 rounded-full bg-black opacity-100"></span>
        </div>
    )
}

export default BlackAnimation